name: Dispatch Workflow with Parameters
on:
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Send Dispatch Event
        run: |
          curl -X POST -H "Authorization: token ${{ env.PAT }}" \
               -H "Accept: application/vnd.github.everest-preview+json" \
               https://api.github.com/repos/krzysztof-gawrys-cognitran/promote-workflow/dispatches \
               -d '{"event_type": "run-tests", "client_payload": {"branch": "develop", "version": "1.2.3"}}'